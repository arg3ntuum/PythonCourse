Завдання 1: Локальні та глобальні змінні
Напишіть програму, яка створює глобальну змінну x і функцію, яка змінює значення x, не використовуючи ключове слово global.
Виведіть значення x до і після виклику функції, щоб показати, як змінюється локальна та глобальна змінна.

Завдання 2: Використання ключового слова global
Напишіть програму, яка створює глобальну змінну count і функцію, яка змінює її значення за допомогою ключового слова global.
Виведіть значення змінної до та після зміни її в функції.
python

Завдання 3: Область видимості змінних у вкладених функціях
Створіть глобальну змінну total і функцію, яка містить вкладену функцію, що змінює total.
Використовуйте ключове слово global, щоб змінити значення глобальної змінної у вкладеній функції.

Завдання 4: Використання глобальних змінних у циклі
Напишіть програму, яка використовує глобальну змінну sum_result і цикли для обчислення суми чисел від 1 до 10.
Змініть значення глобальної змінної всередині циклу.

Завдання 5: Взаємодія глобальних змінних і функцій
Напишіть програму, яка має дві глобальні змінні a та b.
Напишіть функцію, яка використовує обидві ці змінні та повертає їх суму, не змінюючи значення глобальних змінних.